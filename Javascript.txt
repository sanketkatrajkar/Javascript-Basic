
⭐ JavaScript Notes

1️⃣ Variable
A variable is a container used to store data.

| Keyword   | Use            | Notes                                 |
| --------- | -------------- | ------------------------------------- |
| **var**   | Old way        | Function-scoped, can be redeclared    |
| **let**   | Modern way     | Block-scoped, cannot be redeclared    |
| **const** | Constant value | Block-scoped, value cannot be changed |

Examples:
let age = 20;
const PI = 3.14;
var name = "Sanket";

2️⃣ Data Types in JavaScript

JavaScript has 7 primitive data types + Object.

Primitive Data Types:
Number → 10, 3.14
String → "Hello", 'JS'
Boolean → true, false
Null → empty value
Undefined → value not assigned
BigInt → large integers (9007199254740991n)
Symbol → unique identifiers

Non-Primitive:

Object, Arrays, Functions

4️⃣ NaN (Not-a-Number)
NaN is a special value that means → Not a valid number.

When NaN occurs?
"abc" / 2    // NaN (string cannot be divided)
Number("hello") // NaN

Properties:

Type of NaN is number
NaN == NaN → false
Check NaN using isNaN() or Number.isNaN().

5️⃣ Operator Precedence

Operator precedence means → which operator executes first.

Higher precedence executes first.
Common precedence order (high → low):
() Parentheses
Unary operators → ++ -- ! typeof
Multiplication / Division / Modulus
Addition / Subtraction
Comparison operators
Logical AND (&&)
Logical OR (||)

Assignment operators (= += -=)

Example:
let result = 5 + 3 * 2;  // 3*2 = 6 → 5+6 = 11

6️⃣ Unary Operator
Unary operators work on one single operand.

Examples:
let x = 5;
console.log(++x);   // Pre-increment (6)
console.log(x--);   // Post-decrement (prints 6, becomes 5)

Types:

Increment → ++x, x++
Decrement → --x, x--
Unary plus → +x
Unary minus → -x
typeof → check type

delete → delete object property

7️⃣ Null & Undefined
Undefined
Value is not assigned.

let a;
console.log(a);  // undefined

Null
A deliberate empty value.

let b = null;
console.log(b);  // null

| undefined                      | null                        |
| ------------------------------ | --------------------------- |
| Variable declared but no value | Programmer sets it manually |
| System/default empty           | Intentional empty           |
| Type → "undefined"             | Type → "object" (JS bug)    |

7️⃣ console.log() :  To write (log) a message on the console

7️⃣ Template Literals : They are used to add embedded expressions in a string
    eg. console.log( ` Your pay $(a + b) rupees ` );

7️⃣ Comparison of non - Number values is valid in js 
    'a' < 'A' == TURE 

7️⃣ Alert and prompt

Alert Display an alert message on the page  alert("something is wrong);
Prompt Display a dialog box that asks user for some input
eg prompt("Please enter your roll no");

7️⃣ String are Immutable in js 
   No change can be made to string 
   Whenever we do try to make a change a new string created and old one reamins same

7️⃣ String Methos 

 str.trim() - Remove the whitespaces form the both ends of string & reduce a new string 
 let msg = "  Hello  ";
 output = Hello;

7️⃣ String Methos 
 
 str.toUpperCase() : all letter of string is IN UPPER CASE 
 str.toLowerCase() : all letter of string is IN LOWER CASE

7️⃣ slice method : Returns a part of the orignal string as new string 
 eg. let str = "llovecoding";
 str.slice(5)   // coding
 str.slice(1,4) // love
 
7️⃣ Replace : Searches a value in the string & return a new string with the value replaced 
7️⃣ Repeat : Return a string with the number of copies of a string 
7️⃣ String Methods with arguments 
 Arguments is some value that we pass to the method
 indexOff method : Returns the first index of occurrence of some value in string or gives -1 if no found

7️⃣ method chaing 
Using one method after another Order of execution letf to right 
str.toUpperCase().trim()

 

